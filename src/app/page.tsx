import * as React from "react";

const Home = () => {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <span className="text-4xl">Can access without authentication</span>
    </div>
  );
};

export default Home;
